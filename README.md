# JavaFXButtonInTableWithHibernate

## Launching the project
1. Open mysql workbench and run the query: `CREATE DATABASE datademo;`
2. Run the project from intellij (or maven).
3. You should see this view:
